{
  "name": "sublink-plus",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "setup-kv": "node scripts/setup-kv.js"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.20",
    "vitest": "^3.0.0 <3.1.0",
    "wrangler": "^4.13.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "sublink-plus": "file:"
  }
}
